# Travel Agency - freeCodeCamp Project

This project is part of the Responsive Web Design certification on freeCodeCamp.  
It includes:

- Lists with links (`<ul>`, `<li>`, `<a>`).  
- Images with `<figure>` and `<figcaption>`.  
- Semantic and accessible HTML.

You can view it online by opening `index.html`.
